# Red String — agent guide

Two-person iCloud status app (iOS 17+, SwiftUI). One partner sets a status/nudge/photo/doodle/voice memo; it appears on the other's lock screen. No server: everything travels through CloudKit (a zone-wide `CKShare`), human-readable fields end-to-end encrypted via `encryptedValues`. Three shipping targets (app, widget extension, notification service extension) plus a unit-test bundle — all compile `Sources/Shared/`.

`README.md` is the deep reference (design rationale, pairing/share security model, shipping/CloudKit-schema steps, demo mode). Read the relevant README section before changing sync, pairing, or notification behaviour. `ROADMAP.md` lists planned features. This file is the quick map + the invariants that must not break.

Comment style: comments are deliberately lean — one or two lines carrying only the non-obvious constraint or "why". Don't add narrative comments, restate code, or document history; match the existing density.

## Build & run

- `RedString.xcodeproj` is **generated** from `project.yml` by XcodeGen (`make project`) but also **committed** (Xcode Cloud needs it). After adding/renaming files: `make project`, commit the regenerated project too. Never edit project settings in Xcode's UI — they're overwritten.
- `make build` = unsigned compile check (do not launch the product). `make run` = signed build + launch in Simulator. `make device`, `make archive` per README.
- Demo mode for screenshots/UI work: launch with `REDSTRING_DEMO=1` (Debug only) — stub backend + `DemoSeeder` content, no iCloud needed.
- `make test` runs `Tests/RedStringTests` (XCTest). The bundle compiles `Sources/Shared/` directly — no host app, so no signing or App Group — and covers the persistence invariants below: Codable fallbacks, watermarks, `MomentIndex`/`StatusHistoryLog` merging, `SharedStore`, plus the pure decision types the CloudKit code delegates to — `RefreshDelta` (how a delta folds into `Snapshot`), `AnnouncementPolicy` (every notification's check-and-claim) and `WidgetReloadPolicy` (the widget's next refresh) — and smaller pure helpers (`withDeadline`, `confirmSaved`, moderation, anniversary maths, moods, `PairRole`, waveforms). Stores take an injected file URL or `UserDefaults` suite; never touch the real container from a test. Keep decisions in those types rather than inline in `CloudSync.apply`/`SyncRunner`/the NSE, which have no coverage.
- User-facing text is localisable: SwiftUI `Text` literals and `String(localized:)` for everything else (notification bodies, widget strings, accessibility labels). Each target owns a `Resources/Localizable.xcstrings`; an Xcode IDE build fills it (xcodebuild leaves it empty, which is harmless — keys are the English strings). Every icon-only control carries an `accessibilityLabel`; `Theme.rounded` follows Dynamic Type (capped at 1.35×).

## Architecture (data flow)

```
UI (Views/) → AppModel (@MainActor, @Observable)
            → Backend.current (SyncBackend protocol: CloudSync actor, or DemoBackend)
            → CloudKit custom zone "CoupleZone", zone-wide CKShare

SharedStore (App Group, file-backed KV via GroupFileStore)  ← app / widget / NSE all read+write
  ├─ Snapshot        widget-renderable cache: both statuses, nudge watermarks, latest moments
  ├─ PairingInfo     role (owner/participant) + zone coords + account userRecordName
  └─ change tokens   per-database CKServerChangeToken

MomentIndex   moments-index.json in App Group — full history metadata (cap 500)
MomentStore   media files in App Group /Moments — JPEG full (photo's own frame) + square thumb, .m4a (full media capped to the 60 newest, thumbnails kept for the whole index;
              pending-upload media is never pruned)
StatusHistoryLog  status-history.json — local rolling log of both sides' statuses (cap 300: both sides' `statusLogLimit` of 150)
SyncRunner    refresh → decide what to announce → NotificationManager (local notifications)
NotificationService  enriches CloudKit pushes: refreshes, decrypts, rewrites banner, reloads widgets
```

Key types: `StatusPayload` (one per partner, fixed record names `status-owner`/`status-participant`), `StatusLog` (one record per status change, `statuslog-<role>-<seconds>`, the durable history; each side prunes its own past `AppConfig.statusLogLimit`), `Nudge` (own record type so its push can be visible+sound), `Moment` (one record per item, name `moment-<role>-<uuid>`, kept forever — that's what makes history recoverable), `Receipt` (one per side, `receipt-<role>`: encrypted seen-map plus the status read receipt (`statusSeenAt`/`statusSeenFor`), no subscription — arrives with any refresh), `Anniversary` (one per pair, fixed name `anniversary`, owner-written: encrypted `startsAt` + `timeZone`, the clock behind the easter egg; no subscription), `AnniversaryRequest` (one per pair, fixed name `anniversary-request`, participant-written: encrypted `requestedAt` — "please set the date"; no subscription, the owner sees it on their next open). `PairRole` derives all per-side record names; neither device ever needs the other's CloudKit user ID.

## Invariants — do not break

1. **Cross-process locking.** App, widget, and NSE run concurrently on the same triggers. Every read-modify-write of `Snapshot` goes through `SharedStore.mutate` (flock-based `CrossProcessLock`); `MomentIndex` locks likewise. Check-and-claim decisions (announce a nudge/moment, rewrite a banner) must happen *inside* one `mutate`.
2. **Apply before token, and readable before token.** `CloudSync.refresh` folds records into local state *before* persisting the change token. Reversing it loses records forever when the NSE is killed on deadline. A record whose encrypted fields come back empty (`CloudSync.isReadable`, probed on a field every version has always written) is skipped, listed in `RefreshResult.unreadableRecordNames`, and the token is *not* advanced — a background process without the share's keys must not consume a delta it couldn't read, or the words are gone for good. The per-process tally shows in Diagnostics. The one exception is the *foreground app* giving up: `SharedStore.noteUnreadableRecords` counts separate app refreshes (≥ `AppConfig.unreadableHoldSpacing` apart) that skipped the same record names, and after `AppConfig.unreadableHoldLimit` of them returns `true` so the token advances — otherwise one record nobody can ever read pins the token and the whole delta behind it forever. Extensions only add to the tally; they never advance. Unreadable never means absent: `MomentIndex.insert` keeps held captions/names/waveforms over empty ones, `logEntry(from:)` writes no placeholder, and `RefreshDelta` clears the anniversary or status receipt only on an explicit deletion / a readable receipt.
3. **Shared state lives in files, not UserDefaults.** `UserDefaults(suiteName:)` proved unreliable as an App Group channel (see `GroupState.swift`). Widget↔app data goes through `GroupFileStore` / files.
4. **Encryption boundaries.** Text and behavioural fields (`emoji`, `message`, `displayName`, `caption`, `senderName`, `waveform`, `isCelebration`, the receipt's `seenMap`/`statusSeenAt`/`statusSeenFor`, the anniversary's `startsAt`/`timeZone`, the request's `requestedAt`) go through `record.encryptedValues`. Plaintext by design: record names, moment `kind`/`sentAt`/`duration`/`momentID`, nudge `count`/`sentAt`, `updatedAt` — the locked-phone banner depends on `kind` staying readable. `CKAsset`s are encrypted by CloudKit automatically and must NOT go through `encryptedValues`. Adding a plaintext field that carries user words is a privacy regression.
5. **Hand-written Codable.** `Snapshot`, `StatusPayload`, `Moment` decode with `decodeIfPresent` + fallbacks so old on-disk data survives new fields. New persisted fields must follow that pattern (and consider what the fallback means for legacy data).
6. **Widgets reload via `SharedStore.reloadWidgets()`** after any snapshot/media change — but never from inside the widget process (guarded by `isRunningInWidgetExtension`).
7. **Cooldown claims before network.** `CloudSync.sendNudge` claims the cooldown inside the lock before the network call; a failure releases it and stamps `lastNudgeFailedAt` (the lock-screen heart's only error channel).
8. **Unlink is cloud-first.** `unpair()` must succeed before local state is cleared; `forceLocalReset` is the explicit escape hatch. "Zone gone" under a *different* iCloud account must never wipe local state (`SyncError.differentAccount`). **"Zone gone" is a verdict only on a second sighting** (`CloudSync.zoneGoneVerdict`): the first stamps `SharedStore.zoneGoneSeenAt` and throws the transient `SyncError.zoneUnreachable`; only a sighting `AppConfig.zoneGoneConfirmation` later wipes and throws `linkEnded`, and any successful fetch clears the stamp. The invite-close handshake takes the partner off the share for seconds, and one refresh in that window must not cost them their unsent media. When the verdict does land, `eraseLocalMedia(keepingPendingUploads: true)` spares own sends that never reached CloudKit, `clearPairing(keepingName: true)` remembers the zone in `SharedStore.lastPairing`, and `acceptShare`/`rejoin` (via `adoptPairing`) skip the wipe when the accepted zone is that same one — so the re-tapped link re-sends them. A different zone, a new invite, or "start over" still wipes. Mid-refresh guards compare the pairing by `PairingInfo.sameZone(as:)`, not by existence, so an unlink-and-re-pair spanning one refresh can't file the ex's delta or token onto the new pairing (`createPairInvite` clears tokens like `acceptShare` does). A change token cleared *during* `apply` (a corrupt moment index being rebuilt) is not written back. The owner's zone ID names *whoever is signed in*, so `requirePairing` and `unpair` refuse with `differentAccount` on positive proof of a mismatch (cached account lookup, `CloudSync.currentUserRecordName`); an unlink mid-refresh also stops `apply` filing anything past the status write and skips the token persist.
9. **Invite close is a two-step handshake, manual-only.** No single CKShare save converts a link-joined (public) partner to private: an in-place `role` flip is silently ignored (close included), and close+`addParticipant` in one save applies the close but drops the add (both verified against the live service, 2026-09). `lockPairing` therefore closes first (which sweeps the public joiner), then re-adds them via `CKFetchShareParticipantsOperation` (by `userRecordID` — nobody has email/phone `lookupInfo` since iOS 17) as an *invited pending* private participant; the partner confirms by tapping the invite link once (`receiveInvite`'s same-zone branch → `reacceptShare`). Verified end-to-end: link closed, partner private+accepted, sync intact. While pending the partner has no zone access, so the promote runs only from the Diagnostics button with the partner on standby — never from a passive path (auto-close in refresh stays disabled), and its failure path reopens the link (`promoteToPrivate` throwing → `reopenInvite`). Settings' "Close the invite link" is `closeUnusedInvite`, which refuses with `SyncError.inviteInUse` once anyone is on the share — it can never evict. No user-facing copy may say the link closes itself; `SettingsView.inviteFooter` and `InviteLinkSheet` describe the manual flow.
10. **Announcement dedup watermarks** live in `Snapshot`: `lastSeenPartnerNudgeCount`, `notifiedMomentIDs`/`lastNotifiedMomentID`, `lastAnnouncedPartnerStatusAt` + `lastAnnouncedPartnerStatus` (the words, so a rename is judged against what was last *announced*, never against a pre-refresh snapshot the widget may already have updated), `lastCelebratedAt`, `lastAnnouncedMomentSentAt` (the time floor the banner's index fallback searches past; `AnnouncementPolicy.claim` marks *every* moment in a burst announced and `bootstrapAfterPairing` seeds the floor, so neither a burst nor a re-fetched history can be described as new). The NSE and app both advance them; nudge watermark moves with `max()`, never assignment. **Every NSE banner is a claim** (`AnnouncementPolicy.claimStatusBanner`/`claimNudgeBanner`/`claimMomentBanner`); an unclaimed push is never credited to the partner — it is either our own write from a second device on this iCloud account (`RefreshResult.ownRecordsChanged`, judged against the held snapshot, so a full resync doesn't trip it; worded as such) or something another process already announced, and either way it is delivered `.passive` without sound — *except* when the NSE's own refresh skipped unreadable records (`RefreshResult.unreadableRecordNames` non-empty, a locked phone): the partner's event is real and nobody has announced it, so it stays at full volume, worded only from what travels unencrypted: when the unreadable records' *names* show the partner's moment or status, their name plus the moment's plaintext `kind` (`RefreshResult.heldPartnerMomentKinds`/`heldPartnerStatus`, `AnnouncementPolicy.heldMomentBody`), stamped `NotificationCategory.heldBannerKey` so the app's moment sweep supersedes it (the app posts no status notifications, so a held status banner stays); otherwise CloudKit's generic words. `claimMomentBanner` never falls back to re-describing an old moment.
11. **Offline sends recover, never silently drop.** Statuses: `Snapshot.myStatusPublished` + `republishStatusIfNeeded()` (covers the `StatusLog` record too — `publish` writes both, and a failure on either keeps the flag down). Moments: `Moment.uploaded` + `retryPendingUploads()`. The anniversary: `Snapshot.anniversaryPublished` + `republishAnniversaryIfNeeded()` (owner only; `CloudSync.apply` leaves an unpublished local value alone so the republish wins, and only an explicit record *deletion* clears the date — a delivered record whose encrypted fields are unreadable, as happens in the NSE/widget on a locked device, keeps the stored copy, like `payload(from:)` does for statuses). The anniversary request: `Snapshot.anniversaryRequestPublished` + `republishAnniversaryRequestIfNeeded()` (participant only, same fold rule). All re-run safely (fixed record names / overwrite semantics). Media is written locally before any network call. The home footer's "waiting to send" is a button (`AppModel.retryPendingNow`) over the same retry.
12. **`AppConfig` IDs must stay in lockstep** with the four entitlements files (app Debug and Release, widget, NSE), `project.yml`, and `Info.plist` `NSUbiquitousContainers`.
13. **Read receipts are double-gated and sticky.** `readReceiptsEnabled` gates both publishing and display; `Snapshot.receiptsDirty` + `flushReceiptsIfNeeded()` is the retry loop: the flag is claimed before the network call and re-set on failure, so a `markSeen` or toggle landing mid-flight is flushed by the next pass (same shape as the nudge cooldown), re-armed once per launch to heal misses; `MomentIndex.applyPartnerReceipts` never un-sees, and `MomentIndex.insert` keeps every local-only field (`seen`, `seenAt`, `seenByPartnerAt`, `uploaded`) sticky — a moment rebuilt from a CloudKit re-delivery carries none of them. Receipts fold into the index *after* a delta's moments are inserted. Disabling publishes an empty map and no status receipt. The **status** receipt (`Snapshot.partnerStatusSeen`, published; `myStatusSeenByPartner`, received) is stamped only by `AppModel.markPartnerStatusSeen` from the home screen while the app is `.active` — a background refresh is not anyone looking — and only moves forward; it counts for display only while its `statusUpdatedAt` still equals `mine.updatedAt`.
14. **Status history dedups by `(fromMe, updatedAt)` from two sources.** The current `Status` record as it changes (`AppModel.setStatus`, `CloudSync.apply`) and the per-change `StatusLog` records, which collapse into the same entry because both key on the whole-second `updatedAt` — a `StatusLog` record is *named* by it. Pruning is cloud-first: the publisher deletes its own records past the cap, and every device mirrors deletions via `StatusHistoryLog.remove`. Entries from before the cloud log existed are local-only and still don't survive a reinstall.
    **Persisted dates are whole seconds.** All App Group JSON encodes ISO-8601, which drops fractional seconds — a fractional `Date` never equals its own stored copy. `StatusPayload.updatedAt` is therefore truncated at creation (`AppModel.statusTimestamp`) and at CloudKit ingestion (`CloudSync.payload(from:)`); any new date used in an equality/watermark check must follow suit.
15. **Corrupt store files are preserved, not overwritten.** `SharedStore.decode`, `MomentIndex`, and `StatusHistoryLog` move unreadable bytes to a `.corrupt` sidecar before falling back to empty; the moment index also clears change tokens so CloudKit rebuilds it. Keep that pattern for any new persisted file.
16. **Publish-success flags must verify currency.** `markStatusPublished(payload)` only flips `myStatusPublished` if the payload is still the current status — a late-finishing publish must not mark a newer offline edit delivered. `CloudSync.publish` therefore never writes an older payload over `mine`, and `saveStatus` skips when the server copy is newer. Renames publish with `logged: false` (no `StatusLog` record); the receiver logs and announces them as renames, not statuses.
17. **Scrub gestures need `.highPriorityGesture`** (`ScrubbableWaveform`) in the gallery pager: a plain `.gesture` loses to the horizontal paging ScrollView, and waveforms must not be wrapped in a `Button` (see `VoiceMemoRow`). Inside a vertical ScrollView a high-priority drag hijacks scrolling instead — there pass `scrollSafe: true`, which swaps in `HorizontalScrub`, a UIKit pan that fails on vertical movement.
18. **Pin vertical-ScrollView content with `.containerRelativeFrame(.horizontal)`** (see `HomeView`). A child with a wide *ideal* size — a long single-line `Text` — can inflate the scrollable content's horizontal extent on some OS builds, letting the whole screen pan sideways; prefer `.fixedSize(horizontal: false, vertical: true)` on wrapping text too. (`WaveformBars` is safe: it condenses to what fits and never reports more than its measured width.)
19. **Never hold a shared-container file lock without a suspension guard.** Being suspended mid-`flock` is a `0xdead10cc` kill (TestFlight, 2026-09). `CrossProcessLock.withLock` holds a `performExpiringActivity` assertion for its critical section, so it's covered everywhere; async paths that end a UIKit background task (`withUploadProtection`) must do their post-await bookkeeping *inside* the protected closure, not after it.
20. **Moderation is local and sticky (App Review guideline 1.2).** `TermsView` gates everything until `SharedStore.acceptedTermsVersion` reaches `AppConfig.termsVersion`. A reported moment goes into `hiddenMomentIDs`, which `CloudSync.apply` filters from every delta (the record lives on in the sender's iCloud and resyncs re-deliver it); a reported status hides by `hiddenPartnerStatusAt`. Blocking (`AppModel.block`) records the partner in `blockedOwnerRecordNames` *before* the pairing is cleared, wipes locally even if the cloud unpair fails (the one place local-first is right), and `acceptShare`/`discoverExistingPairing` refuse those people. `ContentFilter` is word-level and applied to the partner's text only — UI, notifications and widgets alike — and *only through the presentation helpers in `Moderation.swift`*: `StatusPayload.moderated(reportedAt:)` (reported → no words and no emoji; filtered → placeholder), `StatusHistoryEntry.moderated`, `Moment.displayCaption`/`displaySenderName(fallback:)`, `Snapshot.moderatedPartnerName`. Names are user text and go through them too. A new surface that prints partner text calls one of these; the widget and the NSE read `hiddenPartnerStatusAt` the same way the home screen does. Reports are `mailto:` to `AppConfig.supportEmail` (no server); the 24-hour response is a commitment in the terms and on the site.

21. **No `.safeAreaInset(edge: .top)` over a scrolling list inside a `NavigationStack`.** A TestFlight watchdog kill (2026-09, iOS 26.6) caught the main thread looping between the navigation bar resizing and the sheet's `ScrollViewCommitMutation`: UIKit tracks the content scroll view's offset for the bar, SwiftUI's inset adjusts the same scroll view, and with enough rows to scroll they chase each other. `StatusHistoryView` and `MomentLibraryView` pin their filter picker with a plain `VStack` above the list instead.
22. **A save is confirmed by the record's own result, never by the call returning.** Every `modifyRecords` goes through `CloudSync.confirmSaved`/`confirmDeleted`: the async API throws for the operation, but a failed record can also arrive *inside* the result dictionary, and `_ =` on it marked photos sent on patchy signal delivered when the server never had them (participant side, 2026-09). Recovery is `MomentIndex.requeueMissingUploads`: after a complete full-zone fetch in the app (`previous == nil`, nothing more coming), own moments marked uploaded that the zone didn't return — judged by record *name*, so unreadable copies still count as present — go back in the retry queue if their media is still on disk; Diagnostics' "Re-check sent moments" (`AppModel.resyncHistory`) clears the tokens to force one. Uploads and refreshes are bounded (`AppConfig.uploadDeadline`/`refreshDeadline`): CloudKit's resource timeout is seven days, and a stalled operation held `isRetryingUploads`/`isRefreshing` — and with it every retry — until relaunch.

## File map (one line each)

Shared/
- `AppConfig.swift` — all identifiers + tunables (cooldowns, cache limits, memo max length, status/caption character caps, the unreadable-hold limit).
- `Theme.swift` — colors (`warmDeep` for any orange that carries white text or sits as text on the cream ground; `warm` alone is ~2.6:1 there), card/button styles. Headlines use `Theme.rounded`, never a fixed `.system(size:)`, so Dynamic Type reaches them. `Waveform.swift` — level condensing for memo waveforms.
- `Models/StatusPayload.swift` — `StatusPayload`, `PairRole`, `PairingInfo`, `Snapshot` (the widget cache; read its doc comments before adding fields).
- `Models/Moment.swift` — moment metadata + presentation helpers. `Models/Mood.swift` — status presets. `Models/Anniversary.swift` — the owner-set start date + its calendar/milestone maths (the easter egg's clock). `Models/MomentAttachment.swift` — notification attachment factory.
- `Store/SharedStore.swift` — App Group KV + `mutate` + derived-field recompute (`applyDerived`) + `readReceiptsEnabled`. `Store/GroupState.swift` — file-backed store + one-time UserDefaults migration. `Store/CrossProcessLock.swift` — flock wrapper. `Store/MomentIndex.swift` — history JSON, seen/uploaded/receipt merging. `Store/MomentStore.swift` — media files, thumbnail cache (evicted on delete), prune (5-min grace; spares pending uploads via caller's keep-set, and every indexed thumbnail — the library grid scrolls through them). `Store/StatusHistoryLog.swift` — status log (fed by `Status` + `StatusLog` records).
- Extensions (widget, NSE) take **one batch per refresh** (`fetchZoneChanges(oneBatch:)`, `RefreshResult.incomplete`): they run under a 24–30 MB ceiling and a full-zone resync held every record in memory. The batch's token is a valid cursor, so apply-before-token still holds and the app finishes the resync; the NSE treats an incomplete delta like an unreadable one (generic words, full volume). The widget downloads thumbnails only, and `downloadRecentMedia` stops at cancellation.
- `Cloud/CloudSync.swift` — the CloudKit actor's core: errors, record/field names, account checks, shared helpers (`payload(from:)`). Media downloads copy to a `.part` sibling then rename (`copyAsset`), so a kill mid-copy never leaves a truncated file that `hasMedia` would trust. `pruneStatusLog` deletes non-atomically and only drops locally what the server confirmed gone. One concern per extension file, all still the same actor: `CloudSync+Pairing` (share lifecycle, invite close handshake, zone recovery), `+Status` (publish + `StatusLog`), `+Refresh` (change fetch, `apply`), `+Nudges`, `+Moments` (send, media download, record parsing, tokens), `+Receipts`, `+Anniversary` (owner-only write/delete of the pair's date), `+Subscriptions`, `+Unpairing` (+ `CKError` classifiers). Members are `internal` so the files can see each other; actor isolation still guards state. Most safety-critical code in the repo.
- `Notifications.swift` — notification category/action IDs (shared with the NSE, which stamps them) and the `Notification.Name`s (`pairingDidChange` = re-read *and* refresh, from another process; `snapshotDidChange` = the model's own refresh wrote the store, re-read only).
- `Moderation.swift` — `ContentFilter` (word list + whole-word match), the presentation helpers every surface shows partner text through (`moderated`, `displayCaption`, `displaySenderName`), and `Report` (mail subject/body/URL).
- `Cloud/RefreshDelta.swift` — the parsed delta and its pure fold into `Snapshot` (newer-wins for both statuses, erasures, anniversary + request, status receipt). `AnnouncementPolicy.swift` — watermark check-and-claim for nudges, moments and push banners (`StatusBanner` tells a rename from an update); run inside one `mutate`. `WidgetReloadPolicy.swift` — when the widget refreshes next: hourly when caught up; while records are held unreadable (`UnreadableTally.heldSince`) every 5 min for the first 30, 15 min up to 2 h, then 30; 5 min after an `incomplete` batch — nothing reloads a widget on unlock.
- `Cloud/SyncBackend.swift` — backend protocol + `RefreshResult` (unreadable names, `ownRecordsChanged`, `incomplete`) + `withDeadline` (every widget/intent CloudKit call is bounded by `AppConfig.widgetDeadline`; a kill mid-write would leave claims unreleased. It is a race, not a task group: on the deadline the body is cancelled and abandoned, since auto-imported CloudKit calls ignore cancellation — so `refresh` checks cancellation *before* `apply`, never between apply and the token, and `saveNudge` checks before its save so the cooldown is released) + DEBUG `DemoBackend`. `Cloud/CloudDiagnostics.swift` — Settings→Diagnostics report (includes the unreadable-record tally).

App/
- `RedStringApp.swift` — scene phases, notification-center subscriptions, deep links (`redstring://compose|open`); sets `AppModel.current`. `AppDelegate.swift` — push registration, scene delegate for share acceptance, `InviteInbox`, banner action handling (heart back → `sendNudge`, text reply → `setStatus` with 💬; awaited, since the app may be woken in the background for it). `RedStringShortcuts.swift` — App Shortcuts (Siri) over `SendNudgeIntent`, which `project.yml` compiles into both the app and the widget.
- `AppModel.swift` — the app's single view model: derived accessors, lifecycle refresh, status/nudge/moment send paths with upload protection, invite management, unlink, archive.
- `SyncRunner.swift` — refresh + announce decisions. `NotificationManager.swift` — local notifications (moment/nudge, stale-nudge wording, generic-banner sweep), category registration.
- `MemoryArchive.swift` — exports history to iCloud Drive as plain files + HTML/text index. `DemoSeeder.swift` — DEBUG demo content.
- `Audio/VoiceRecorder.swift`, `Audio/VoicePlayer.swift` — AVFoundation record/playback.
- `Views/` — SwiftUI screens: `RootView` (routing: welcome→pairing→home; `presentsModelErrors()` — every sheet HomeView presents hosts the model's error alert, since one presentation per view means the root's alert can't show over a sheet; root-level sheets wait on `AppModel.homeSheetShowing`), `HomeView` (status row with "Seen …" receipt, partner card → status history, nudge, moment card, memo row; composer deep-link queueing; stamps the status read receipt), `SettingsView` (name, notifications, safety: filter toggle/block/terms/report, read-receipts toggle, invite, archive, unlink, diagnostics), `TermsView` (the 1.2 terms gate, also read-only from Settings), `WelcomeView`/`PairingView`/`InviteLinkView` (onboarding + invite), `MoodPickerView` (status presets + celebration), `MomentComposerView`+`DrawingCanvas`+`CameraPicker` (photo/doodle), `VoiceMemoComposerView`+`VoiceMomentViews`+`ScrubbableWaveform` (memos + swipe-to-seek), `MomentLibraryView`/`MomentGalleryView` (history grid/pager, direction filter, seen badges; the pager is a horizontal paging `ScrollView` + `LazyHStack`, never a page-style `TabView` — that one builds every page up front and halts halfway between pages when any page's content changes mid-swipe, a photo finishing its decode included; both verified on iOS 26), `HistoryFilter` (shared filter enum+picker), `StatusHistoryView` (status log sheet), `CelebrationOverlay` (also owns the reusable `ConfettiLayer`), `TieTheStringView`+`LogoView`+`AnniversaryView` (the easter egg, `EasterEggView`: a 1.2 s long-press on the home title → drag the red string from the fox to the fish across the riverbank scene → the pair becomes the logo, which waits → a 1.2 s long-press on the logo → a live count from the owner-set `Anniversary`, with milestones; empty state until it's set; `Resources/Assets.xcassets/Logo` is app-only), `AnniversaryEditorView` (owner only: the prompt after creating the link, the row a 1.2 s long-press on the Settings title reveals, and the count screen's "set the date" button), `RelativeTime` (every ticking "3 minutes ago" — anchored to the date's own minute boundaries; `Date.relativeWording()` for one-shot strings, so VoiceOver and the screen agree), `PinchToZoom`, `ShareSheet`, `DiagnosticsView`.

Widget/
- `RedStringWidgetBundle.swift` — bundle. `StatusWidget.swift`+`WidgetViews.swift` — status widget (accessory families + systemSmall) and `NudgeWidget` (the lock-screen heart, accessoryCircular). `MomentWidget.swift` — photo widget (systemSmall/Medium/Large, memo badge, compose deep link). `StatusProvider.swift` — shared timeline provider (8s CloudKit timeout, cooldown flip-back entries; next refresh from `WidgetReloadPolicy`). `SendNudgeIntent.swift` — lock-screen heart AppIntent.

NotificationService/
- `NotificationService.swift` — push enrichment; dispatches on `subscriptionID` (never on sync delta), claims banners under the cross-process lock, stamps `NotificationCategory` so the actions appear.

Tests/RedStringTests/ — XCTest over `Sources/Shared` (see "Build & run"). `TestSupport.swift` has the fixtures and throwaway-file helpers.

docs/ — GitHub Pages marketing site (`index.html`), deployed by `.github/workflows/static.yml`. Unrelated to the app binary.

## CloudKit schema gotchas (summary — full detail in README "Shipping it")

- Production auto-creates nothing: deploy schema after Development use, and `cloudkit.share` only enters the schema when a share is actually saved in a Debug device build. The `Receipt`, `StatusLog`, `Anniversary` and `AnniversaryRequest` types likewise only appear after a Debug device has saved one (view a received moment with read receipts on; set a status; answer the date prompt as the owner; tap "Ask … to set it" on the count screen as the participant), and `Receipt`'s `statusSeenAt`/`statusSeenFor` fields only once a status has been on screen — deploy before shipping.
- Field encryption is fixed at creation; the CloudKit environment (Dev vs Prod) is decided by code signing, and mixed-environment devices can't see each other (looks like a broken pairing — see `CloudDiagnostics`).
- A second device on the *same* iCloud account (a Simulator signed in as you, another iPhone) is the same CloudKit user with the same `PairRole`: it writes `status-<role>`, `nudge-<role>` and `moment-<role>-…`, i.e. *your* records, and its writes fire *your* subscriptions. They show up as your own status/sends, never as the partner's, and the NSE words those pushes as "from another device" (invariant 10).
- Subscriptions: `status-alerts` (visible, silent-sound), `nudge-alerts`, `moment-alerts` (both visible + sound + mutable content); legacy `status-changes` is deleted on sight.
